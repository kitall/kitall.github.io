SELECT * FROM tabelaA a INNER JOIN tabelaB b
    ON a.id = b.id WHERE ...
    
/*
    INNER JOIN = INTERSECÇÃO das duas tabelas -> produtos que aparecem juntos com os usuarios
    OUTER JOIN = a+b-(a e b) -> todos que não/nunca apareceram juntos
*/
/*
    Olhar a imagem: sql joins
*/